name: '2024-medalists-all'
version: '1.0.0'
config-version: 2

profile: 'operation_medallion'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  2024-medalists-all:
    bronze:
      +materialized: table

    silver:
      +materialized: table
      +transient: false

    gold:
      +materialized: table
